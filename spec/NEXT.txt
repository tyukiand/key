Iteration: implement spec/0012-natural-feature-ids.txt — rename
`Feature::canonical_id` from auto-derived `category-name`
(e.g. `pseudo-file-env`) to hand-chosen natural search terms
(e.g. `env`). Pure user-facing string change; AST and Feature
roster unchanged.

Authoritative spec: spec/0012-natural-feature-ids.txt (six
sections). Background: spec/0011-guide-tighter-terse.txt §B.3
introduced `canonical_id`; this iteration replaces its
algorithmic derivation with a hand-written table per the
disambiguation rule in §1.2.

Hard rules:
  - Append " (done)" to each numbered item below as it finishes,
    or " (skipped: <reason>)" if obsolete. Do not delete prior
    markers.
  - Build with cargo; `./test.sh` runs the suite.
  - DO NOT change the Feature enum roster, parent map, or
    AST→Feature mapping. This iteration is purely the string
    table behind `canonical_id`.
  - DO NOT edit spec/0011 or spec/0010 — increments are
    immutable post-release.

1. Replace `Feature::canonical_id` with a hand-written match
   per spec §1.1 / §1.2. One string literal per Feature. (done)
2. Apply the disambiguation rule (§1.2): proposition-level
   `all` / `any` / `not` / `conditionally` get the `-prop`
   suffix; predicate-level versions take the bare names. (done)
3. Extend the existing uniqueness test (from spec 0011 step
   1) to assert the new ids are still pairwise unique. (done)
4. Sweep the codebase: search for the old id strings in
   tests, in EDSL prose, in documentation, in error messages.
   Replace each with the new id per spec §1.1. (done)
5. Update the rerun-line emission (spec 0011 §C.2) so the
   `--feature=<root-id>` portion uses the new ids in actual
   output. Verify visually with
   `key audit guide | grep "rerun"`. (done)
6. Implement the did-you-mean hint in the unknown-id error
   (spec §3.1): when the typed id has a known canonical id as
   a suffix (after stripping a `-`-separated prefix), suggest
   the matched short id. No fuzzy-match crate. (done)
7. Tests for §4:
     - uniqueness (§4.1);
     - functional regression: each renamed id is reachable
       via `--feature=<id>` and produces the same content
       the old id produced (compare against pre-rename
       fixture, or assert structural equivalence) (§4.2);
     - did-you-mean: `--feature=pseudo-file-env` errors with
       hint `did you mean: env?`; same check for
       `predicate-shell-exports-variable` →
       `shell-exports`, `proposition-forall` → `forall`
       (§4.3);
     - terse rerun lines now contain the new short ids
       (§4.4). (done)
8. Run `./test.sh` and confirm PASS. (done)
9. Run `cargo build --release` and confirm clean. (done)
10. git add -A && git commit -m "key: rename Feature canonical_ids to natural short forms (env, forall, shell-exports, …); did-you-mean for old ids".
