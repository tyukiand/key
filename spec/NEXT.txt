Iteration: implement spec/0014-safe-exec-kernel.txt — funnel ALL
external-process invocation in `key` through one `safe_exec` in a
new `src/security/exec.rs` module, gated by an exhaustive
`AllowedCommand` enum with branded string newtypes.

Authoritative spec: spec/0014-safe-exec-kernel.txt (eight
sections). Background inventory of current exec sites:
  - src/effects.rs (~5 sites: which, ssh-keygen generate /
    fingerprint, ssh-add add / list)
  - src/rules/pseudo.rs (~1 site: <executable:NAME> version
    probe)
  - src/rules/scenario_integ.rs (~7 sites: subprocess-invoking
    the audit binary itself in tests)

Hard rules:
  - Append " (done)" to each numbered item below as it
    finishes, or " (skipped: <reason>)" if obsolete. Do not
    delete prior markers.
  - Build with cargo; ./test.sh runs the suite.
  - DO NOT change user-visible CLI behavior. Pure refactor;
    every existing test must keep passing.
  - DO NOT edit any prior shipped spec increment.
  - The no-direct-exec test (spec §4.1) is the load-bearing
    invariant — implement it FIRST so the migration lights up
    every remaining call site as a failure until it's gone.

1. Create src/security/exec.rs with the module skeleton:
   public AllowedCommand enum (initially empty / placeholder),
   safe_exec stub, brand types module. (done)
2. Implement the no-direct-exec test (spec §4.1): walks
   src/**/*.rs, greps for std::process::Command etc.,
   asserts zero matches outside src/security/. Allowlisted
   path: src/security/exec.rs only. The test FAILs until the
   migration is complete — that's the point. (done)
3. Add the AllowedExecutableName brand type per spec §3.1:
   private inner String, fallible new() with the regex from
   the spec, as_str() accessor. BrandError enum starts with
   the InvalidExeName variant. Tests for the constructor
   (happy path, every documented invalid shape, boundary
   cases). (done)
4. Add the remaining brand types per spec §3.1:
   AllowedKeyPath (must be inside ~/.key/keys/, no .., no
   symlinks beyond), AllowedComment (printable ASCII, length
   cap, no shell-meta), AllowedKeyType (closed set: ed25519,
   rsa-4096, ecdsa-p256, ed25519-sk), AllowedVersionFlag
   (closed set: --version, -version, -V, --help). Each gets
   constructor tests per spec §6.1. (done)
5. Add AllowedSelfArgs (enum of test-harness invocation
   shapes) by walking src/rules/scenario_integ.rs to enumerate
   the actual argv shapes the integration tests use today.
   No arbitrary-string passthrough; tests get an enum
   variant per real shape. (done)
6. Define AllowedCommand variants per spec §2.1, one per
   distinct invocation shape across the existing call sites.
   Mapping is 1:1; if a site cannot be expressed as a finite
   variant, that's a finding to surface (file an OUT-OF-SCOPE
   note and propose a follow-up; do not introduce arbitrary
   passthrough). (done)
7. Implement safe_exec per spec §5: translates the
   AllowedCommand variant into (program, argv); minimal env
   (PATH inherited only by default); per-call timeout (30s
   default; per-variant overridable via in-code declaration);
   bounded stdout/stderr capture (1 MiB per stream;
   truncation marker on overflow); SafeExecResult { exit,
   stdout, stderr, command_summary }. Debug-level logging of
   the variant name (not args). (done)
8. Migrate src/effects.rs: rewrite each Command::new site as
   AllowedCommand construction + safe_exec call. Tests still
   green. (done)
9. Migrate src/rules/pseudo.rs: same. The version-probe loop
   becomes AllowedCommand::ProbeVersionGeneric (or
   per-extractor variants per spec §2.1). (done)
10. Migrate src/rules/scenario_integ.rs: same, using the
    AllowedSelfArgs enum from step 5. (done)
11. Run the no-direct-exec test (step 2). It MUST now pass.
    If it doesn't, find the missed site and migrate it
    before continuing. (done)
12. Run ./test.sh and confirm every pre-existing test
    continues to pass (regression for §6.4). (done)
13. Run cargo build --release and confirm clean. (done)
14. Run ci.sh end-to-end LOCALLY (cargo clean first, then
    fmt + build --release + test) and confirm clean — this
    is the new memory-bound rule, do not skip. (done)
15. git add -A && git commit -m "key: safe-exec kernel — all subprocess invocation funneled through AllowedCommand + branded string newtypes; no direct std::process::Command outside src/security/". (done)

No outstanding work.
