Iteration: implement spec/0011-guide-tighter-terse.txt — tighten
the terse pass (one canonical example per root, everything else
detail), add a `--feature=<id>` filter flag, enforce the same-root
invariant for collapsed detail blocks, and have rerun lines name
the shared root so users can target it.

Authoritative spec: spec/0011-guide-tighter-terse.txt (five
sections A–E, plus background). Background: spec/0010-guide-edsl.txt
shipped at commit 1fe6c18; everything from 0010 stays — this is
purely an editorial-policy tightening + one new flag + one new
invariant.

Hard rules:
  - Append " (done)" to each numbered item below as it finishes,
    or " (skipped: <reason>)" if obsolete. Do not delete prior
    markers.
  - Build with cargo; `./test.sh` runs the suite (it adds
    `--features testing` automatically).
  - DO NOT change the Feature roster, parent map, or AST→Feature
    mapping — those shipped in 0010 and stay.
  - DO NOT edit spec/0010-guide-edsl.txt — increments are
    immutable post-release.

1. Add `Feature::canonical_id(self) -> &'static str` (per
   spec §B.3). Lowercased + hyphenated variant name. Test that
   ids are unique across all Features (parser-friendly form). (done)
2. Sweep the EDSL tree (src/guide_edsl/tree.rs or wherever
   the static GuideNode tree lives): for every ROOT Feature,
   keep AT MOST ONE inline `ExampleControl` (the simplest /
   bare-string form). Flip every other example, refinement
   variant, and predicate-combinator demo to `detail: true`.
   Per spec §A.3. (done)
3. Verify quantitatively after step 2: `key audit guide` line
   count noticeably below 459 (target ~150-250 per §A.4).
   Document the new line count in your scratch. (done — terse: 160 lines)
4. Implement the `--feature=<id>` CLI flag (spec §B). Filter:
   keep only EDSL subtrees whose declared Features
   (recursively) include either the named Feature OR any
   descendant of it in the forest. Combinable with `-v`. (done)
5. Implement the unknown-id error path (spec §B.4): non-zero
   exit, error message lists at least the root feature ids. (done)
6. Implement the same-root invariant for collapsed detail
   blocks (spec §C.1, C.3): when the terse interpreter is
   about to fold consecutive detail subtrees into one rerun
   line, group them by root; emit ONE rerun line per root.
   The previous (0010) behavior of emitting one comma-
   separated line per contiguous run becomes per-root. (done)
7. Update the rerun-line format to include
   `--feature=<root-id>` so users can drill in directly
   (spec §C.2):

     > rerun `key audit guide -v --feature=pseudo-file-env`
     > for more info on ... (done)

8. Tests for §B.5:
     - `--feature=<root>` shows root + descendants in the
       chosen pass;
     - `--feature=<descendant>` shows ONLY that node;
     - unknown id → non-zero exit, error names the
       available roots;
     - filter combines correctly with `-v`. (done)
9. Tests for §C.4:
     - two-roots fixture: contiguous detail subtrees from
       different roots → terse emits TWO rerun lines, one
       per root;
     - one-root fixture: ONE rerun line with shared root;
     - every rerun line in the terse output names a valid
       `--feature=<id>` that, when used, actually expands
       the referenced content (round-trip). (done)
10. Run `./test.sh` and confirm PASS. (done)
11. Run `cargo build --release` and confirm clean. (done)
12. git add -A && git commit -m "key: tighten guide terse pass; add --feature filter; same-root invariant for collapsed blocks".
