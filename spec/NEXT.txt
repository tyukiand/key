Iteration: implement spec/0016-os-effects-and-project-only.txt
— introduce OsEffects capability handle (Ro/Rw split, mock
backend, single injection at main), and make Project the
only unit `key audit` operates on (remove single-file
add/delete/list/new; replace with `key audit project edit`
fdisk-style menu).

Authoritative spec: spec/0016-os-effects-and-project-only.txt
(four sections A–D). Background: spec/0014 (safe_exec stays;
moves to OsEffects method), spec/0015 (Project ADT,
Interaction coroutine, AsmOp — all the foundations are
already in place).

Hard rules:
  - Append " (done)" to each numbered item below as it
    finishes, or " (skipped: <reason>)" if obsolete. Do not
    delete prior markers. NO skipping CLI-removal items
    (spec §B.1) — they are the point of this iteration.
  - Build with cargo; ./test.sh runs the suite.
  - All Command::new still routes through OsEffects (which
    delegates to safe_exec); src/security/exec.rs's
    invariant from spec/0014 is preserved.
  - Run ci.sh end-to-end LOCALLY before pushing.

1. Define `OsEffects` trait + Ro/Rw marker traits per
   spec §A.1, §A.2. Place under src/effects/os.rs (or
   similar). All methods listed in §A.1 included. (done)
2. Implement RealOsEffects per spec §A.3 — std::fs +
   safe_exec wrappers. Tests for happy-path file IO and
   safe_exec dispatch. (done)
3. Implement MockOsEffects per spec §A.3 — in-memory FS
   keyed by path string, programmable command results,
   frozen clock, fake env. Declarative seeding from YAML
   fixtures (the existing spec/0009 fixture format).
   Tests for every method. (done)
4. Add no-direct-fs lint test (`tests/no_direct_fs.rs`)
   per spec §A.6: greps src/**/*.rs for std::fs::,
   tokio::fs::, fs::File::; allowlists src/effects/*.rs
   and src/main.rs only. (done)
5. Migrate every std::fs::* call in src/ outside the
   allowlist to OsEffects methods (per spec §A.6). Run
   the lint test after each batch; it's the load-bearing
   invariant. Includes Project::load_from_dir,
   Project::write_to_dir, run_tests's tempdir
   materialization. (done)
6. Inject OsEffects at the single entry point in
   src/main.rs per spec §A.4. NO global / NO static / NO
   thread-local. Every code path that touches the OS
   takes the handle as a parameter. (done)
7. Move safe_exec onto OsEffects per spec §A.1: the free
   function from spec/0014 becomes
   `OsEffects::safe_exec(&self, cmd: AllowedCommand)`. All
   call sites updated; src/security/exec.rs houses only
   the AllowedCommand enum + brand types now (the actual
   exec lives in RealOsEffects). (done)
8. Remove the old `key audit add/delete/list/new`
   subcommands (spec §B.1). Each is replaced by an exit-
   non-zero migration message naming the new
   `key audit project edit`-based entry point. Tests:
   each removed subcommand exits non-zero with a clear
   error. (done)
9. Implement `key audit project edit <dir>` as the new
   live interactive surface (spec §B.2, §B.3): load
   Project, present the menu with full kebab-case
   command tokens — help / list / add-control /
   delete-control / add-fixture / delete-fixture /
   add-test-entry / delete-test-entry / run-tests /
   run-audit / save / quit. NO single-letter aliases,
   NO abbreviations beyond what the Lexical resolver
   already provides for prefix-unique matches.
   Drive a top-level `Interaction<LowLevelInput,
   Project>` with sub-Interactions for the Add*/Delete*
   dialogs. (done)
10. Test ops (`t`) and run-audit ops (`r`) per spec §B.3:
    they don't mutate the Project; they compute a report
    via Project::run_tests / Project::run_audit_against
    _filesystem (which now go through OsEffects) and
    print it inline. (done)
11. Quit-while-dirty confirmation per spec §B.3 (`q`):
    asks "discard unsaved changes? [y/N]" if dirty. (done)
12. Round-trip extension per spec §B.5: every
    AsmOp sequence over the new top-level surface
    (including DeleteControl, RunTests, RunAudit, Write,
    Quit) round-trips correctly. Report ops are
    observational — round-trip equality compares only
    the final Project state, not the printed reports. (done)
13. Migration of in-tree audit fixtures: any fixture or
    test that relied on the removed single-file
    subcommands is rewritten to use Projects. If a
    fixture genuinely makes no sense as a Project, it's
    deleted with a one-line note in the commit message. (done: scenario_unit + scenario_integ migrated; no other fixtures relied on the removed surfaces.)
14. `key audit run <dir>` shortcut per spec §B.6: KEEP as
    thin alias for `key audit project run`. Tests
    confirm equivalence. (done)
15. End-to-end test per spec §D: load empty Project →
    `add-control` CTRL-1 (full Add-Control sub-dialog)
    → `run-tests` (passes) → `save` → reload → assert
    Project state matches the edits. Driven via
    MockOsEffects so the test is hermetic. (done)
16. Run ./test.sh and confirm PASS. (done)
17. Run cargo build --release and confirm clean. (done)
18. Run ci.sh end-to-end LOCALLY (cargo clean → fmt →
    release build → tests) and confirm clean. (done)
19. git add -A && git commit -m "key: OsEffects capability handle (Ro/Rw split, mock backend, single injection); project-only audit CLI (audit project edit fdisk-style menu); single-file add/delete/list/new removed".
